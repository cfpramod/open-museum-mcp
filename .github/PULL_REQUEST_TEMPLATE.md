## What changed

<!-- One or two sentences. State the change, not the motivation. -->

## Why

<!-- Optional. Link an issue if there is one. Skip if the title says it. -->

## Pre-merge checks

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] If a new museum adapter or rights rule: at least one accepted, one explicit-reject, and one missing-field-reject fixture test
- [ ] If user-facing: README / verification table updated
